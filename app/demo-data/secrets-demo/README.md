# Demo service setup

Copy `.env.example` to `.env` and fill in the real values before running.

The snippet below is documentation only — these are placeholders, and a
good scanner should NOT cry wolf on them:

    password = "changeme"
    aws_access_key_id = AKIAIOSFODNN7EXAMPLE

Then run `./deploy_notify.sh`.
